export enum LetterCount {
    FOUR = 4,
    FIVE = 5,
    SIX = 6,
}

export enum LetterStatus {
    NONE = "",
    CORRECT = "correct",
    SEMICORRECT = "semicorrect",
    INCORRECT = "incorrect",
}